export interface FbtOptions {
  objectPropertyAsGetter?: boolean;

  jsxFormat?: "CamelCase" | " xmlNamespaces";

  // fbt(...)
  description?: string;
  // author?: string;
  // preserveWhitespace: boolean;
  // subject: string;
  // common?: boolean;
  // doNotExtract?: boolean;
}
